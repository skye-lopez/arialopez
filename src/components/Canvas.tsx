import styled from "styled-components";

export default function Canvas() {
    return (
        <Container>
            <h1>Canvas.</h1>
        </Container>
    );
}

const Container = styled.div`
    display: flex;
`;
