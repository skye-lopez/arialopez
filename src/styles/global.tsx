import { createGlobalStyle } from "styled-components";

const global = createGlobalStyle`
    html, body { 
        padding: 0;
        margin: 0;
        color: white;
    }
`;

export default global;
